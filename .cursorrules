# 全域開發與安全守則 (Agent Rules)

1. 敏感檔案保護（最高優先級）：
   - 嚴禁修改、覆寫或刪除任何 .env、.env.local 或包含金鑰的環境設定檔。

2. 增量開發原則：
   - 進行任何功能新增或 UI 調整時，必須保留既有運作正常的業務邏輯，嚴禁整檔覆寫或刪除既有功能。
   - 除非使用者明確要求重構，否則不得更換既有的框架（如 Tailwind、Next.js/React 版本）。

3. 自我驗證閉環：
   - 每次執行完檔案修改或套件安裝後，必須自動執行 npm run build / lint 進行編譯檢查，若有報錯需自行修復至完全通過，最後僅回報完成摘要。

4. 破壞性指令防護：
   - 嚴禁執行 rm -rf、git reset --hard 或任何不可逆的刪除指令。
