{
  "name": "taiwan-pt-web",
  "version": "1.0.0",
  "description": "台灣物理治療助手 — 公開查詢服務",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "test": "node scripts/verify-all.cjs",
    "preview": "node scripts/verify-all.cjs && vercel",
    "deploy": "node scripts/verify-all.cjs && vercel --prod",
    "promote": "node scripts/verify-all.cjs && vercel --prod"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@line/bot-sdk": "^9.3.0",
    "@vercel/analytics": "^2.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "devDependencies": {
    "puppeteer-core": "^24.43.1",
    "sharp": "^0.35.3"
  }
}
